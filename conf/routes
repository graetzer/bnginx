# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index
GET     /search                                 App.Search
GET     /page/:pageId/*                         App.Page

# Login & Logout
POST    /login                                  App.Login
POST    /logout                                 App.Logout

# =========== Admin section ===========
GET     /admin                                  Admin.Index

# User management
GET      /admin/user/:email                    Admin.EditUser
POST     /admin/user/save                      Admin.SaveUser
GET      /admin/user/delete/:email             Admin.DeleteUser

# Post management
GET     /admin/post/:postId                     Admin.EditPost
POST    /admin/post/:postId                     Admin.SavePost
GET     /admin/post/delete/:postId              Admin.DeletePost

# Media upload
GET     /admin/upload                           Admin.Upload
POST    /admin/upload/save                      Admin.SaveUpload

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
# *       /:controller/:action                    :controller.:action
